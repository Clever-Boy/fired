SUBDIRS := ${shell find * -type d -print}
OBJS    += $(SUBDIRS:=.o)



all: $(PROJECT)


$(PROJECT): subdirs $(OBJS)
	$(CC) $(OBJS) -o ../$(PROJECT) $(LDFLAGS)
	$(CLEAN) $(OBJS)


subdirs: $(SUBDIRS)
	for dir in $(SUBDIRS); do \
		export SUBDIR=$$dir; \
		$(MAKESUB); \
	done
