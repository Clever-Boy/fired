OBJS   := $(wildcard *.cpp)
OBJS   := $(OBJS:.cpp=.o)
OUT    := $(SUBDIR).o



all: $(OUT)


$(OUT): $(OBJS)
	$(LD) -r $(OBJS) -o ../$(OUT)
	@echo


.cpp.o:
	$(CC) $(CFLAGS) -o $@ -c $<

clean:
	$(CLEAN) $(OBJS)
